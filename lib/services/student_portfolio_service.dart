import 'package:flutter/foundation.dart';

/// Student Portfolio Service Stub
class StudentPortfolioService {
  Future<List<dynamic>> getPortfolioItems(String studentId) async {
    debugPrint('⚠️ StudentPortfolioService: Supabase migration pending');
    return [];
  }

  Future<void> addPortfolioItem(Map<String, dynamic> item) async {
    debugPrint('⚠️ StudentPortfolioService: Supabase migration pending');
  }
}
